# MCP 2026-07-28 upgrade

This change adds a native 2026-07-28 server path while preserving the
initialization-based revisions as a compatibility adapter. The two eras share
tool/resource/prompt managers and JSON-RPC framing, but they do not share
lifecycle state.

## Wire behavior

| Concern | 2026-07-28 | Compatibility adapter |
|---|---|---|
| Negotiation | `server/discover` or inline request metadata | `initialize` + `notifications/initialized` |
| Client state | Version, identity, capabilities, and log level in every request | Stored on `Session` |
| HTTP lifecycle | Independent POSTs; optional request-scoped SSE | `Mcp-Session-Id`, GET stream, DELETE |
| Additional client input | MRTR `input_required` + retry | Correlated server-initiated request |
| Subscriptions | `subscriptions/listen` POST stream | Resource subscribe/unsubscribe + GET stream |
| Tasks | `io.modelcontextprotocol/tasks` bearer handles (any transport); invisible to legacy sessions | `params.task`, list/get/result/cancel (session-scoped; rejected on the shared stateless session) |
| Results | Required `resultType`; cache hints where specified | Earlier wire shapes unchanged |
| Tool handler errors | Handler-raised `McpError` escapes as a protocol error (`-32021` → HTTP 400) | Always in-band `CallToolResult.isError` |
| Resource not found | Invalid Params (`-32602`) with `data.uri` | Reserved `-32002` with `data.uri` |
| Modern-only methods | Dispatched (`server/discover`, `subscriptions/listen`, `tasks/update`) | `-32601` Method Not Found |
| Capability gating | Per-request, incl. sub-capabilities (`elicitation.url`, `sampling.tools`/`.context`); `-32021` | Initialize-time, base capabilities only; `-32600` |

Modern HTTP requests require `Content-Type`, an `Accept` value that supports
JSON and SSE, `MCP-Protocol-Version`, and `Mcp-Method`. `Mcp-Name` is required
for tool calls, resource reads, and prompt gets. Header-backed tool arguments
are validated from `x-mcp-header` annotations, including Base64 sentinel
decoding and numeric integer comparison.

The `stateless` setting now affects only older HTTP traffic. All 2026 requests
are stateless, ignore `Mcp-Session-Id` and `Last-Event-ID`, and never mint a
session. Modern GET and DELETE requests receive 405.

## Review map

- `core/Protocol.scala`, `jsonrpc/McpError.scala`, and `core/wire/Envelopes.scala`:
  version, allocated errors, request/result shapes, cache fields, and
  subscriptions.
- `server/router/RequestContext.scala`, `McpRouter.scala`, and `McpContext.scala`:
  per-request isolation, version dispatch, result metadata, MRTR, and logging.
- `server/router/Builtins.scala`: the era seams — the tools/call error
  contract (in-band vs protocol error) and the resource-miss code split.
- `server/transport/HttpHeaderValidation.scala` and
  `server/transport/ModernHttpValidation.scala` plus the JVM/Bun backends:
  header/body validation, version classification, stateless modern routing,
  response status mapping, and response-stream cancellation.
- `server/router/TaskRouting.scala` and `server/manager/TaskManager.scala`:
  official extension handles, bearer lookup, polling, cancellation, and the
  isolated legacy task path.
- `Protocol20260728Test`, `JvmHttpTransportTest`, and `JsServerHttpTest`:
  executable protocol fixtures and cross-platform transport behavior.

## Deliberate boundaries

- Roots, Sampling, and Logging remain available because the new revision
  deprecates rather than removes them. New server code should prefer explicit
  tool/resource parameters, direct provider APIs, and stderr/OpenTelemetry.
- `subscriptions/listen` is fully lifecycle-safe and acknowledges only
  notification types the server can emit. The library does not yet expose
  dynamic list/resource-change publishers, so those filters are declined.
- The Tasks extension currently produces working/completed/failed/cancelled
  tool tasks. `tasks/update` validates the protocol shape, but the server does
  not yet suspend task execution in `input_required`. Task-status notifications
  (`notifications/tasks/status`) are emitted on legacy 2025-11-25 sessions on
  every terminal transition; modern bearer tasks do not emit them — the
  2026-07-28 extension moved status to polling (`tasks/get`).
- Authorization and client-registration changes are outside this server
  library's current surface; no OAuth authorization server or MCP client
  credential store is implemented here.

## Verification

```bash
./mill --no-server fast-mcp-scala.compile
./mill --no-server fast-mcp-scala.test
./mill --no-server fast-mcp-scala.reformat
git diff --check
```

The focused suites are:

```bash
./mill --no-server fast-mcp-scala.jvm.test.testOnly \
  com.tjclp.fastmcp.server.Protocol20260728Test
./mill --no-server fast-mcp-scala.jvm.test.testOnly \
  com.tjclp.fastmcp.server.transport.JvmHttpTransportTest
./mill --no-server fast-mcp-scala.js.test.testOnly \
  com.tjclp.fastmcp.conformance.JsServerHttpTest
```

## 1.0.0-RC1 gate ledger

How each RC1 criterion was met (decisions confirmed by the maintainer,
2026-08-17, tracked as TJC-2000):

- **Official 2026-capable conformance oracle** — met via
  `@modelcontextprotocol/conformance@0.2.0-alpha.11`, the first release
  carrying the 2026-07-28 scenario set. Both platforms pass
  `scripts/conformance.sh {jvm|js} <port> 2026` (`--requirements 2026-07-28`,
  every scored scenario green; the failing scenarios are ones the harness
  itself marks `extension`/`pending` and does not score). The stable TS SDK
  (1.30.0) still declares 2025-11-25, so the alpha was accepted as the RC
  oracle; **re-validate against a stable oracle before 1.0.0 final**.
- **Harness pin** — the conformance oracle version lives in
  `conformance/package.json`; bump it and regenerate `conformance/bun.lock`
  with the Mill-managed Bun in a reviewed PR (the lock diff is the review
  surface — `--frozen-lockfile` enforces the lock, not package.json).
- **External client end-to-end** — the conformance harness's synthetic client
  drives discovery, list/call/read/get, MRTR (single, multi, multi-round,
  tampered-state), and the HTTP header-negative cases. Validation by an
  independent external client against the published RC artifact is exactly
  what the RC period is for.
- **2025-11-25 compatibility matrix** — the active suite runs every scenario
  on both protocol eras (73 checks, empty baselines, JVM + Bun), and the JS
  ConformanceTest drives the real TS SDK 1.29.0 client over stdio. *(Corrected
  in the 1.0.0 ledger below: the active suite's 31 scenarios / 73 checks all run
  at the 2025-11-25 wire; the 2026-07-28 wire is exercised by the `2026` mode.)*
- **Soak test** — `LifecycleSoakTest`: 25 dropped `subscriptions/listen`
  streams and 10 TTL-expired running tasks leave no fibers or task entries
  behind.
- **Tasks limitations** — ship documented (maintainer decision): the extension
  stays opt-in and off by default; `tasks/update` input resumption and modern
  task-status notifications remain unimplemented and documented. The
  `tasks-*` conformance scenarios are unscored extensions.
- **Version move** — `build.mill` default moves `0.5.0-SNAPSHOT → 1.0.0-RC1`
  in this release-preparation commit; the tag `v1.0.0-RC1` publishes with the
  prerelease flag, and a post-release commit bumps the dev default.

## 1.0.0 gate ledger

How the final-release criteria were met (decisions confirmed by the maintainer,
2026-09-04 and — decisions (a)–(j) of the pre-release session — 2026-09-08,
tracked as TJC-2277). The rows were completed in the R3 block after the last
pre-tag fix PR (#107) merged into the release branch; the two items that can
only happen after that merge — regenerating `conformance/api-1.0.0.txt` from
the final tree and the maintainer's dashboard triage — are named where they
belong:

- **Conformance oracle** — the RC1 ledger asked for re-validation against a
  stable oracle before 1.0.0. Checked 2026-09-04: the stable
  `@modelcontextprotocol/conformance` release is still 0.1.16, which carries no
  2026-07-28 scenarios, and the stable TypeScript SDK (1.30.0) still declares
  2025-11-25. `0.2.0-alpha.11` therefore remains the pinned oracle for 1.0.0;
  three RCs (2026-08-17 → 2026-08-31) ran against it with every gate green on
  JVM, Bun, and the GraalVM image. The pin is bumped deliberately when a stable
  2026-capable release appears. What the oracle measures (alpha.11 bundle
  inventory, 2026-09-08): 62 server scenarios in all; `--suite active` (the
  `scripts/conformance.sh <platform>` default and the `conformance.yml` gate) is
  31 scenarios / 73 checks, every one at the 2025-11-25 wire — no active
  scenario is `introducedIn: 2026-07-28`, so the active gate sends no 2026-07-28
  request; `--requirements 2026-07-28` (the `2026` mode, gated on every PR for
  the JVM and Bun since TJC-2356 / PR #108, and as a JVM-vs-native parity diff
  in `native.yml`) runs 50 scenarios of which 37 are scored (118 checks) and 13
  are reported but never scored (10 `tasks-*` extension scenarios, which the
  stock `ConformanceServer` runs with tasks off, plus `json-schema-2020-12`,
  `http-custom-header-server-validation` and `server-sse-polling`, whose
  fixtures it lacks); the harness's raw `Total:` line in that mode counts the
  unscored checks too, so the gate reads the scored 37/37. With tasks enabled and
  fixtures registered (a scratch server, both platforms identical
  check-for-check) the ten `tasks-*` scenarios score 27/44: `tasks-wire-fields`,
  `tasks-request-state-removal` and `tasks-required-task-error` fully green
  server-side, the rest failing only on the five SEP-2663 deviations listed
  under *Tasks* and on one harness-side validator gap (`wire-schema-valid` has no
  `CreateTaskResult` branch on alpha.11). Release-tree results (R3, 2026-09-08):
  active 73/73 on JVM, Bun and the GraalVM HTTP image; `2026` 37/37 scored on
  JVM and Bun.
- **RC period** — RC1 (2026-08-17), RC2 (2026-08-28), RC3 (2026-08-31); no
  protocol regressions reported against the published artifacts.
- **Security** — the TJC-2294 hardening wave ships in 1.0.0: 12 findings from
  the 2026-09-04 scan, five arcs (TJC-2295 core input limits, TJC-2296 HTTP
  transport, TJC-2297 tasks, TJC-2298 macro binding, TJC-2299 CI supply chain),
  PRs #87–#92 merged as a stack with merge commits — `LimitSettings`,
  full-origin `HostGuard` + `allowedOrigins`, the 415/413 gates, `maxSessions`,
  the bounded Tasks store, exact-overload macro binding, SHA-pinned actions and
  the frozen conformance harness (CHANGELOG `[1.0.0] ### Security`). The
  supply-chain items are CI-only by nature (shell and workflow YAML, no unit
  test; actionlint clean, zizmor no high findings). GitHub's dependency graph
  cannot see Mill-resolved Maven dependencies, so the Maven tree is audited
  against OSV explicitly (see *Dependencies*); GitHub's own vulnerability alerts
  would cover Actions and bun only and were not enabled on the repository when
  the release gate ran (the alerts API answered 404 on 2026-09-08) — the OSV
  workflow is the CVE feed the release relies on, and enabling the GitHub alerts
  is a maintainer setting, not a code change (waived for 1.0.0; 1.0.1 checklist
  item). The two HostGuard parsing gaps the dogfood's property tests found
  after the wave (multi-valued `Host` matched on its first value; a malformed
  `Host` port admitted when no `Origin` was sent) are closed by TJC-2354
  (PR #106) — fail-closed on both headers, each judged on its own. Marking the
  12 findings *Fixed* on the claude.ai Security dashboard is the maintainer's
  step once PR B has merged (R4, `claude-security-triage`).
- **Dependencies** — netty 4.2.17.Final over zio-http 3.4.0 (TJC-2327, PR #97):
  `Versions.netty` plus the 16 `io.netty:*` modules zio-http brings declared
  directly in `jvm.mvnDeps` and a `bomMvnDeps` import of `io.netty:netty-bom`;
  `resolvedMvnDeps` shows one netty version (20 jars, all 4.2.17.Final, no
  4.2.3). How the published POM carries it (audited on the release-tree
  rehearsal publish, R3): the 16 modules are plain `<dependency>` entries at
  4.2.17.Final, and Mill 1.1.8 renders the `netty-bom` import as an
  import-scoped `<dependency>` of type `pom` *inside* `<dependencies>` — the POM
  has no `<dependencyManagement>` element, contrary to what PR #97's description
  said. Coursier consumers (sbt, Mill, scala-cli) resolve every io.netty module
  at 4.2.17.Final (28/28 rows, classified native artifacts included); Maven and
  Gradle consumers get the 16 directly declared modules by nearest-wins, while
  their treatment of the import-scoped entry (and therefore of any netty module
  zio-http reaches only transitively) is unverified — a 1.0.1 item is to render
  a proper `<dependencyManagement>` import or drop the BOM entry. The OSV audit of the resolved
  tree went from 27 advisories (12 HIGH) to 0 across 40 coordinates and is now a
  CI gate — `.github/workflows/osv.yml` (TJC-2329, PR #101) queries OSV.dev for
  every coordinate on the three production classpaths weekly, on every PR that
  touches the build files and on demand, failing on MODERATE or higher
  (`scripts/osv-scan.sh` runs it locally). zio-http itself stays 3.4.0: the
  version carrying netty 4.2.17 upstream would move zio and zio-json across all
  three platforms — evaluated for 1.1.0. The shed proof holds on the resolver
  (`cs resolve ... --exclude dev.zio:zio-http_3 --exclude 'io.netty:*' | grep -c
  io.netty` → 0) and end to end: the documented two-exclusion recipe
  (Mill `.exclude("dev.zio" -> "zio-http_3", "io.netty" -> "*")`, sbt
  `.exclude("dev.zio", "zio-http_3").exclude("io.netty", "*")`, verified in the
  consumer-recipe matrix) links a 33 MB stdio-only GraalVM image with no
  `io.netty` / `zio.http` strings that answers initialize, `tools/list` and
  `tools/call`; the native HTTP image passes conformance 73/73 on netty 4.2.17
  with the flags tuned on 4.2.3.
- **Deprecations** — the two members deprecated at RC1
  (`ErrorCodes.ResourceNotFound`, `ElicitRequestUrlParams.requiredError`) and
  the `HostGuard.isAllowed(host, origin, allowed: Set[String])` overload
  deprecated during the RC4 development cycle (its `since` named a release that
  was never published; the CHANGELOG promised removal in 1.0.0) are removed
  rather than carried through the 1.x compatibility window; none shipped in a
  stable release (DEPENDENCY_POLICY.md). Decision (a).
- **API surface** — `com.tjclp.fastmcp.examples.*` and `com.tjclp.fastmcp.macros.*`
  are published but outside the 1.x compatibility contract (CHANGELOG
  *Upgrading*, DEPENDENCY_POLICY.md). The MiMa baseline for 1.1.0 is 1.0.0;
  RC3 is not a baseline (TASTy 28.9; `McpServerSettings` grew from 18 to 22
  constructor parameters and then lost the six never-read ones). PR #102
  (TJC-2336; decisions (c) and (d)) landed before the tag: the root import
  `com.tjclp.fastmcp.{*, given}` also exports `TaskSettings`, `LimitSettings`,
  `TaskSupport`, `TaskOwnerKey`, the `ResourceContents` /
  `TextResourceContents` / `BlobResourceContents` payload ADT, and the 22
  `core` / `core.wire` names a handler must spell to call the public
  `McpContext` and completion surface (`CreateMessageRequestParams`,
  `CreateMessageResult`, `SamplingMessage`, `ModelPreferences`, `ModelHint`,
  `ToolChoice`, `ElicitRequestParams`, `ElicitRequestUrlParams`,
  `ElicitResult`, `ListRootsResult`, `Root`, `Implementation`,
  `ClientCapabilities`, `LoggingLevel`, `ProgressToken`,
  `CompleteRequestParams`, `CompletionReference`, `PromptReference`,
  `ResourceTemplateReference`, `CompletionArgument`, `CompletionContext`,
  `Completion`); `core.wire.Tool` stays unexported because it would collide with
  the `@Tool` annotation. `RootImportExportsTest` (JVM) and its Bun and Scala
  Native twins type-check the documented fences under the root import alone;
  every doc fence keeps its named import as a valid alternative. The six
  never-read `McpServerSettings` fields and `McpServer.dependencies` are removed;
  the six inert `@Tool` / `ToolDefinition` metadata fields and `ToolExample` are
  `@deprecated("metadata only; not emitted on the wire; removed in 2.0.0",
  "1.0.0")` and stay accepted through 1.x. Two `### Changed` bullets are
  deliberately CHANGELOG-and-scaladoc-only rather than guide material:
  `ResourceTemplatePattern` becoming a `final class` (its `matches` returning
  `Option[Map[String, String]]`) and description-only `@Tool` / `@Prompt`
  registering under the method name — the latter also in docs/upgrading.md
  §5.3. The public-symbol inventory that seeds
  the MiMa baseline and its exclude list is committed as
  `conformance/api-1.0.0.txt`; it was generated from the dogfood build of main
  and is regenerated from the final release-branch tree in R3 stage 2, now that
  the last fix PR has merged.
- **Build flags** — `-experimental` dropped in TJC-2335 (PR #98, merged; decision (h)):
  the five `Symbol.info` call sites in `MacroUtils` / `ToolProcessor` — the only
  `@experimental` reflect member the annotation macros used — became the stable
  `Symbol.termRef.widenTermRefByName`, and the flag left `build.mill`, so no
  published definition is stamped `@experimental` any more (TASTy stamp check:
  0/241 `_3`, 0/251 `_sjs1_3`, 0/237 `_native0.5_3` files carry `Added by
  -experimental`; the RC builds were 228/228). Gates on the PR: JVM 483, Bun 70
  and Scala Native 14 tests green, conformance 73/73 on JVM and Bun, and both
  consumer probes (README annotation quickstart, `McpTool` typed contract)
  compile and serve without the flag. Every consumer snippet, script and recipe
  in this repository drops `-experimental`; the Scaladoc-as-description
  fallback (`Symbol.docstring`, stable) is unchanged. Consumer snippets no
  longer pass `-Xcheck-macros` either; the library build keeps it (decision (e)).
- **Tasks** — legacy 2025-11-25 sessions emit `notifications/tasks/status` on
  every terminal transition (`completed`, `cancelled`, `failed`; a failing tool
  body is a *completed* task with in-band `isError`) and modern bearer tasks do
  not; both are spec-conformant for their era, so the docs were corrected and no
  flag was added (decision (g); pinned by the drafted JVM
  `TaskStatusNotificationTest`, one assertion due in 1.0.1). `input_required`
  suspension remains unimplemented. Measured against the SEP-2663 texts of the
  harness's five `tasks-*` scenarios (extension-sourced; none of them is in the
  73/73 gate), 1.0.0 deviates in five places, all tracked for 1.0.1 and listed
  in docs/spec-coverage.md: (1) `tasks/get`, `tasks/update` and `tasks/cancel`
  are served to a request that does not carry the `io.modelcontextprotocol/tasks`
  client capability (no `MissingRequiredClientCapability` error); (2) `Mcp-Name:
  <taskId>` is not validated on the tasks-namespace methods; (3) a legacy
  `params.task` on a 2026-07-28 request is rejected with `-32602` instead of
  tolerated and ignored; (4) `tasks/cancel` on a terminal task answers `-32602`
  instead of an idempotent empty ack; (5) `tasks/update`'s `resultType:
  "complete"` is unreachable because no task enters `input_required`.
  Wire-visible but not a SEP item: the 2025-11-25 `tasks` capability advertises
  only `requests.tools.call` although `tasks/list` and `tasks/cancel` are
  served. Task cancellation is cooperative (a synchronous body observes the
  interrupt when it returns; docs/tasks.md), and a `tasks/result` for a task
  cancelled by `tasks/cancel`, the TTL sweep or its session's release now
  answers `-32602` instead of hanging (TJC-2353, PR #104, all three platforms).
  The official harness's `tasks` suite is not yet gated (`scripts/conformance.sh`
  runs the stock `ConformanceServer` without `TaskSettings`, and the harness
  scores extension scenarios in no mode — see *Conformance oracle* for the
  measured 27/44 with tasks enabled); the lifecycle is covered by the in-repo
  JVM, Bun and Scala Native tests — 1.0.1 issue for the gate (register the
  tasks fixtures in `ConformanceServer` together with the SEP-2663 fixes).
- **Settings docs** — `exposeTemplatesEndpoint` (default `false`) and
  `disallowDelete` (default `false`) are documented in docs/transports.md;
  defaults unchanged (decision (i)).
- **Release pipeline** — Dependabot #95 merges first so the release workflow's
  first run uses the action pins CI has already exercised on `main`; v1.0.0 is
  the first run of the split `verify` → `publish` → `github-release` workflow
  (rewritten 2026-09-04, not executed since) and the first Central publish of
  `fast-mcp-scala_native0.5_3`. Accepted with the written rollback protocol in
  CONTRIBUTING.md (nothing reaches Central before the last step of `publish`; a
  failed verify/publish is fixed forward and re-tagged; once anything is on
  Central the fix ships as 1.0.1) instead of an RC4 rehearsal; the workflow's
  `workflow_dispatch` dry run (TJC-2329, PR #101: the dispatch runs the tests
  and `publishLocal` of all three artifacts on the runner, never the Sonatype
  step) exercises it on GitHub runners before the tag — the dispatch itself is
  the maintainer's step (CONTRIBUTING.md "Releasing"; no dispatch run had been
  made when the R3 gate closed on 2026-09-08, so it sits on the pre-tag
  checklist). Release notes are
  curated post-tag from the `[1.0.0]` CHANGELOG
  section with `gh release edit --notes-file`; `--notes-start-tag` is deferred
  to 1.0.1 (decisions (f), (j)).
- **External clients** — run in the 2026-09-08 dogfood against the build of
  main the release branch started from, each recorded with the era it negotiated
  (the two client-visible defects these runs found — omitted defaults not
  applied, TJC-2334, and `tasks/result` never answering after cancel/TTL,
  TJC-2353 — are fixed in PRs #103 and #104, see *Fixes*):
  - **Claude Code 2.1.263** — streamable HTTP in the **2026-07-28** era
    (`server/discover`, `tools/list`, `resources/list`, `prompts/list`,
    `tools/call greet` → `Hello, Ada!`; every POST answered 200
    `text/event-stream`; no session, no GET channel, no `Origin`); stdio in the
    **legacy 2025-11-25** era (`initialize`, then list + call green) against the
    JVM `AnnotatedServer`, the scala-cli README quickstart, the Scala.js/Bun
    bundle, the Scala Native binary and the GraalVM stdio image — no orphaned
    server process, stdout clean on every run.
  - **MCP Inspector 2.5.0 (CLI)** — legacy era (bundles core 2.0.0,
    `2025-11-25`): the README recipes (HelloWorld, `search`, `greeting`,
    `echo`) and the tjc-agents upgrade probe (`add(21, 21)` → `42`) over stdio.
  - **Go SDK 1.7.0** — streamable HTTP in the **2026-07-28** era against the JVM
    and Bun conformance servers (`server/discover` first, no `initialize`
    fallback; `tools/list` (23), `tools/call`, `resources/read`, `prompts/get`,
    MRTR elicitation and sampling, `input_required` results; every POST with
    `Mcp-Method`/`Mcp-Name` and `Mcp-Protocol-Version: 2026-07-28`), and stdio
    `CommandTransport` against the JVM and Scala Native `AnnotatedServer`
    (connect, `tools/list` (5), `tools/call add` → `5`, child exits 0 on stdin
    close).
  - **Java SDK 2.0.1** (`mcp-core` + `mcp-json-jackson3`) — streamable HTTP in
    the **legacy 2025-11-25** era against JVM and Bun: `initialize` +
    `notifications/initialized`, the GET push stream (JVM; Bun answers 405 by
    design and every operation still succeeded), `tools/list`, `tools/call`,
    `resources/read`, `prompts/get`, `logging/setLevel`, `completion/complete`,
    `resources/subscribe` + `unsubscribe`, `ping`, DELETE and the `-32001` 404 on
    reuse of the deleted session; `Content-Type: application/json;
    charset=utf-8` accepted, `charset=iso-8859-1` refused 415.
  - **TypeScript SDK 1.29.0** — the Bun conformance test client over stdio
    (`js.test`, 17 scenarios) and a legacy tasks client (create, `tasks/get`,
    `tasks/cancel`, `tasks/result`, large and slow sampling replies).
  - **Codex CLI 0.153.4** — stdio, README quickstart: `tools/list` and
    `tools/call add` green, no orphan.
  - **Claude Desktop** — not run (human-only step; its recipe in the README is
    the same scala-cli quickstart Claude Code and Codex ran).
- **Known limitations** — shipped documented; 1.0.1 unless another release is
  named. Standing: no MCP client (caps SDK tiering at Tier 3); no dynamic
  subscription publishers (`subscriptions/listen` declines those filters); no
  task `input_required` suspension and no `notifications/tasks/status` for
  modern bearer tasks; Scala Native streamable HTTP deferred to 1.1.0 (GH #81 /
  PR #84); the harness's `tasks` suite not gated; Node (18 and 26) verified for
  the Scala.js stdio bundle only ad hoc in the dogfood, not in CI. Found by the
  dogfood: (1) the SEP-2663 Tasks-extension deviations listed under *Tasks*;
  (2) a modern per-request SSE response is opened with its first frame, so
  `keepAliveInterval` heartbeats do not flow during a silent long tool call
  (legacy POST/GET streams heartbeat from the first byte; docs/transports.md);
  (3) over HTTP a large server→client result (`sampling/createMessage`,
  elicitation) meets `maxRequestBodyBytes` (1 MiB) before `limits.maxFrameChars`
  and is refused with a bodiless 413 by netty/Bun — a 2026-07-28 client fails
  fast, a legacy client waits out `McpContext.DefaultRequestTimeout` (60 s);
  remedy documented (raise `maxRequestBodyBytes` together with
  `maxFrameChars`), a JSON-RPC 413 the legacy client can surface is 1.0.1;
  (4) legacy session store: a request in flight is not session activity and
  gets no reply if its session is evicted or deleted mid-flight (`-32001` reply
  in 1.0.1); a GET peer that vanished without closing its connection is detected
  by the OS only when the server writes to the socket (`keepAliveInterval`) and,
  since TJC-2355 (PR #107), is reclaimed at the `maxSessions` cap once idle past
  `sessionIdleTimeout`; zio-http 3.4.0 exposes no accepted-socket option, so the
  server sets no TCP keepalive; (5) task cancellation is cooperative — a
  synchronous body observes the interrupt when it returns, never on Scala.js
  (grace-bounded cancel in 1.0.1); (6) on Bun a legacy session has no GET
  channel, so out-of-request server→client sends queue for the session's
  lifetime and `sendRequest` from a task body times out after 60 s instead of
  failing fast (push-channel policy in 1.0.1); (7) JVM/Bun parity gaps are
  confined to refusal shapes — empty `mcp-session-id` (404 vs 400),
  PUT/PATCH/OPTIONS/HEAD (404 vs 405), stateless GET/DELETE with a refused
  `Host` (405 vs 403), a trailing slash on the endpoint (JVM lenient, Bun
  exact), GET/DELETE without a session answering 405 where the spec says SHOULD
  400 — every 2026-07-28 row is byte-identical on both backends; neither backend
  adds CORS headers or answers `OPTIONS` preflights (front browser clients with a
  proxy); (8) stdio: replies to frames still in flight at stdin EOF can be
  dropped on the JVM and GraalVM (and on Scala Native for bodies longer than its
  ~8 ms teardown), and JVM/GraalVM stdio servers act on SIGTERM/SIGINT only after
  stdin EOF — hosts wait for every reply and close stdin before signalling
  (interruptible reader in 1.0.1); (9) the `-32600 "Server not initialized —
  send initialize"` answer to a bare frame on a never-initialized session steers
  a 2026-07-28 client to the legacy verb (wording, 1.0.1); the
  Scaladoc-as-description fallback splices the `/** */` delimiters into the wire
  description (1.0.1); the GraalVM stdio image prints JDK 25 `sun.misc.Unsafe`
  warnings on stderr at start (documented; stdout stays clean); (10) protocol
  negotiation for the older legacy revisions (`2024-10-07`, `2024-11-05`,
  `2025-03-26`) is pinned by the dogfood's HTTP matrix (each `initialize`
  answers with the requested version; `tools/list` on a `2024-11-05` session
  carries no post-2024 keys), not by a unit test — the test is a 1.0.1 item;
  (11) build hygiene: `publishVersion` reads `PUBLISH_VERSION` inside a cached
  Mill task, so a warm `out/` can serve a stale value locally (the release
  workflow runs on a fresh runner and asserts the version; a `Task.Input` is the
  1.0.1 fix — clear `out/fast-mcp-scala/*/publishVersion.json` before a local
  `PUBLISH_VERSION=` run). Deferred by decision: `--notes-start-tag` for the
  release notes (1.0.1); a MiMa gate, runnable Scala.js entry points and the TS
  SDK test-client bump (1.1.0); a composable Bun fetch handler / route hook for
  CORS and health routes (1.1.0).
- **Fixes landed before the tag** — from the 2026-09-08 dogfood, merged
  bottom-up with merge commits (PR B, #99, merges main after the last of them
  and moves every `[Unreleased]` bullet into `[1.0.0]`): #95 Dependabot
  actions-group bump; #97 netty 4.2.17.Final + `netty-bom` (TJC-2327); #96
  examples and code comments (TJC-2330); #98 drop `-experimental` (TJC-2335);
  #103 Scala default arguments applied and `@Param` no longer re-requiring
  `Option` on the annotation path (TJC-2334); #102 API surface — root exports
  incl. the `McpContext`/completion wire shapes, never-read settings removed,
  inert `@Tool` metadata deprecated (TJC-2336); #100 Bun SSE keepalive reply
  race + `idleTimeout: 0` (TJC-2337); #104 `tasks/result` frame after
  cancel/TTL/session release (TJC-2353); #101 OSV advisory gate + release
  `workflow_dispatch` dry run (TJC-2329); #105 stdio stderr logger + atomic
  `writeLine` (TJC-2338); #106 fail-closed `Host` parsing (TJC-2354); #107
  idle GET-holder eviction at `maxSessions` (TJC-2355); #108 the 2026-07-28
  conformance requirements run gated on every PR for the JVM and Bun, and
  `pipefail` on the native parity step (TJC-2356).
- **Scope** — Scala Native streamable HTTP (GH #81 / PR #84) is deferred to
  1.1.0; it is purely additive.
- **Artifacts** — `./mill -i __.publishLocal` assembles `_3`, `_sjs1_3`, and
  `_native0.5_3` (the first publish of the Scala Native artifact); the release
  workflow runs the three-platform test aggregate before publishing.
- **Version move** — `build.mill` default moves `1.0.0-RC4-SNAPSHOT → 1.0.0` in
  this release-preparation commit; the tag `v1.0.0` publishes as a full release
  and a post-release commit bumps the dev default to `1.0.1-SNAPSHOT`.
- **SDK tiering** — governance files, issue templates, and the standardized
  triage labels landed in #86; a `tier-check` run against the JVM conformance
  server is recorded under `conformance/` after the release.
