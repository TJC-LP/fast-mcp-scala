# MCP conformance expected-failures baseline — JVM (streamable HTTP).
#
# The JVM transport passes the ENTIRE active server suite (31 scenarios / 73 checks, all at the
# 2025-11-25 wire through the legacy adapter) including the server->client scenarios (sampling /
# elicitation / progress / logging) which stream on each request's own SSE response, and every
# scored scenario of the `--requirements 2026-07-28` run (37/37; that run has no baseline). No
# expected failures.
server: []
