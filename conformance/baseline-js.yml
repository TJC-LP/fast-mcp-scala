# MCP conformance expected-failures baseline — JS (Scala.js / Bun HTTP).
#
# The Bun streamable transport streams every server->client message (progress, sampling,
# elicitation, logging) on each request's own POST SSE response — identical semantics to the JVM
# transport, reusing the shared Session/router logic. JS passes the entire active suite (31
# scenarios / 73 checks, all at the 2025-11-25 wire) and every scored scenario of the
# `--requirements 2026-07-28` run (37/37; that run has no baseline). No expected failures.
server: []
